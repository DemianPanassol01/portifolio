<% if(success && success.length) {%>
    <div class="msg-conf msg-conf-success">
        <%= success %>
    </div>

    <% } %>

        <% if(error && error.length) {%>
            <div class="msg-conf msg-conf-error">
                <%= error %>
            </div>
            <% } %>